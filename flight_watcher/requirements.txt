# Pinned so an unattended 2.5-month run is reproducible and can't break on a
# silent upstream upgrade. Re-pin deliberately when you choose to upgrade.
# Used only by the keyless fallback source (not needed for the Amadeus path,
# which uses the Python standard library).
fast-flights==3.0.2
primp==1.3.1
typing_extensions==4.15.0
